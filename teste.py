import numpy as np
n = 3
# log_c = np.zeros(n)
# matriz_de_operacoes = np.identity(n)

# aux = np.vstack((log_c, matriz_de_operacoes))
# o = np.zeros(shape=(3,5))

# t = np.hstack(())
# print(aux)

print(-np.ones(5))